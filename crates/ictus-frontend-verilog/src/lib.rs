//! Verilog frontend: parses source via `sv-parser` and lowers the result
//! into `ictus_ir`.
//!
//! Wiring up `sv_parser::parse_sv` and the actual AST -> IR lowering is the
//! first real task of phase 1; this crate is currently just a scaffold with
//! the dependency wired in.

pub fn placeholder() {}
